// fixme: not working. some strange error occurred.

// var webpack = require('webpack');
// var WebpackDevServer = require('webpack-dev-server');
// var webpackConfig = require('./webpack.config');
// var settings = require('./config/settings');
//
// new WebpackDevServer(webpack(webpackConfig), settings.serverSetting)
// .listen(settings.port, 'localhost', function (error) {
//     if (error) {
//         console.log(error);
//     }
//
//     // console.log('Please open http://localhost:' + settings.port + '/webpack-dev-server/');
// });

